# Сортировка выбором (Selection sort)
# Так как после каждого прохода по внутреннему циклу делается только один обмен, то общее число обменов равно N-1, что в N/2 раз меньше, чем в сортировке пузырьком.
# O(n ** 2)


def select_sort(lst):
    for i in range(len(lst) - 1):  # пробегаем до предпоследнего элемента
        for j in range(i + 1, len(lst)): # пробегаем по оставшимся, выбираем минимальный элемент, сравнивая два элем-та
            if lst[i] > lst[j]: # если элемент слева больше элемента справа
                lst[i], lst[j] = lst[j], lst[i] # меняем их местами

    return lst


lst = [6, 0, 77, 12, 2]
print(select_sort(lst))
